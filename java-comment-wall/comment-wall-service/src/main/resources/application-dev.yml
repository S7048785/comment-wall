comment:
  wall:
    datasource:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://127.0.0.1:3306/comment_wall
      username: 
      password: 
    jwt:
      # jwt签名加密时使用的密钥
      secret-key: 
      # jwt过期时间 2小时 (=2 * 60 * 60 * 1000)
      ttl: 7200000
      # 设置前端传递过来的令牌名称
      token-name: token
