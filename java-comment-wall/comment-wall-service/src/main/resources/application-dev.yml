comment:
  wall:
    datasource:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://127.0.0.1:3306/comment_wall?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=UTC
      username: root
      password: 5211314
    jwt:
      # jwt签名加密时使用的密钥
      secret-key: awsl
      # jwt过期时间 2小时 (=2 * 60 * 60 * 1000)
      ttl: 7200000
      # 设置前端传递过来的令牌名称
      token-name: token